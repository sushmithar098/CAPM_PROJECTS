_schema-version: 3.3.0
ID: task5
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: task5-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: task5-auth
      - name: task5-db
      - name: task5-destination

  - name: task5-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: task5-db

  - name: task5-app-deployer
    type: com.sap.application.content
    path: gen
    requires:
      - name: task5-auth
      - name: srv-api
      - name: task5-html5-repo-host
        parameters:
          content-target: true
          config:
            HTML5Runtime_enabled: true
    build-parameters:
      build-result: app/
      requires:
        - name: task5adminprofile
          artifacts:
            - adminprofile.zip
          target-path: app/
        - name: task5adminusers
          artifacts:
            - adminusers.zip
          target-path: app/
        - name: task5user
          artifacts:
            - user.zip
          target-path: app/

  - name: task5adminprofile
    type: html5
    path: app/adminprofile
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []
  - name: task5adminusers
    type: html5
    path: app/adminusers
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []
  - name: task5user
    type: html5
    path: app/user
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []

  - name: task5-destinations
    type: com.sap.application.content
    requires:
      - name: task5-auth
        parameters:
          service-key:
            name: task5-auth-key
      - name: task5-html5-repo-host
        parameters:
          service-key:
            name: task5-html5-repo-host-key
      - name: srv-api
      - name: task5-destination
        parameters:
          content-target: true
    build-parameters:
      no-source: true
    parameters:
      content:
        instance:
          existing_destinations_policy: update
          destinations:
            - Name: task5-html5-repository
              ServiceInstanceName: task5-html5-repo-host
              ServiceKeyName: task5-html5-repo-host-key
              sap.cloud.service: task5.service
            - Name: task5-auth
              Authentication: OAuth2UserTokenExchange
              ServiceInstanceName: task5-auth
              ServiceKeyName: task5-auth-key
              sap.cloud.service: task5.service

resources:
  - name: task5-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: task5-${org}-${space}
        tenant-mode: dedicated
        oauth2-configuration:
          credential-types:
            - "binding-secret"
            - "x509"
        role-collections:
          - name: 'admin (task5 ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.admin'
  - name: task5-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: task5-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            existing_destinations_policy: update
            destinations:
              - Name: srv-api
                URL: ~{srv-api/srv-url}
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
                HTML5.ForwardAuthToken: true
                HTML5.DynamicDestination: true
    requires:
      - name: srv-api

  - name: task5-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
